spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/edu
    username: edu
    password: edu123
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  data:
    redis:
      host: redis
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5

jwt:
  secret: ${JWT_SECRET:changeme-dev-only-do-not-use-in-production}
  expiration: 86400000

minio:
  endpoint: http://minio:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: edu

kkfileview:
  base-url: http://localhost:8012

logging:
  level:
    com.example.edu: debug
    org.springframework.security: debug
